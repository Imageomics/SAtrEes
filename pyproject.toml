[project]
name = "satrees"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "saev",
]

[tool.uv.sources]
saev = { git = "https://github.com/Imageomics/saev.git" }
